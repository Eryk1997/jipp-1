#include <iostream>
//#include "trojkat.h"
//#include "sl.h"
#include "triangle.h"
#include "rectangle.h"
using namespace std;

int main()
{
    Rectangle r1;
    r1.area();

/*
    slWindow(500,500, "Trojkat", false);
    slSetBackColor(0.0,0.0,0.0);
    Trojkat tr;

    while(!slShouldClose() && !slGetKey(SL_KEY_ESCAPE) ){
        tr.Rysuj();
        slRender();
    }
    slClose();
*/
    return 0;
}
